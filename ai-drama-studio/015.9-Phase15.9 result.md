Phase 15.9 Complete Report
1. Preflight Audit
Already Complete（15.8 已落地，本阶段未重做）
Script Confirm、Storyboard Generate / Preview / Apply / Confirm（E01：32 shots，READY）
Storyboard JSON 修复、按 scene 重试、Schema Validation、Apply≠Confirm（DRAFT→Confirm→READY）
Episode Workspace / Season-Episode 路由、Core Resolver（Stage / NextAction / Checklist）
Per-shot IMAGE、Per-block TTS、Timeline Builder（幂等 rebuild）、RenderJob / Artifact / FFmpeg 引擎
Apply 幂等、历史 GenerationTask 保留
Partially Complete（本阶段已硬化）
并发重复 Generate 无防护 → 已加 GENERATION_ALREADY_RUNNING
canLockTimeline 在素材缺失时仍为 true → 已与 Visual/Audio 完整度对齐
缺失素材仍可 LOCK → TIMELINE_INCOMPLETE 拒绝
Music 默认 expected=1 误阻塞 MVP → musicExpected=0
Assets 页缺失清单不够清晰 → 增加 Missing Visual / Dialogue 列表
Blocked（环境，非代码假完成）
IMAGE Provider 未配置 → IMAGE_PROVIDER_NOT_CONFIGURED
TTS Provider 未配置 + Character Voice 未配
本机无 FFmpeg / FFprobe
Not Implemented（诚实缺口）
无 multipart Asset 上传兜底 API（无法用 USER_UPLOAD 绕过 IMAGE/TTS）
无「一键只生成全部缺失项」的批量 API（页面引导到分镜/剧本；服务端已是 per-shot / per-block）
2. Production State（Runtime）
项	值
Episode
E01 · 星系碰撞（demo-ep-01）
Project
星河碰撞
Season
第一季
Production Stage
VISUAL_ASSETS
Next Action
GENERATE_MISSING_VISUAL_ASSETS
Script
READY ✓
Storyboard
READY · 32 shots ✓
Visual
0 / 32
Audio
0 / 19 dialogue
Timeline
DRAFT（不可 Lock，缺失素材）
Render
未开始 · canRender=false
3–8. 流水线摘要
环节	结果
Script
PASS（Confirmed）
Storyboard Preview/Apply/Confirm
PASS（15.8 已验收）
Visual Partial Regen（代码）
架构 PASS；Runtime BLOCKED（无 IMAGE Provider）
Audio Partial Regen（代码）
架构 PASS；Runtime BLOCKED（无 TTS / Voice）
Timeline Build
DRAFT 存在；Lock 被正确拒绝 TIMELINE_INCOMPLETE
Timeline Rebuild Idempotency
既有能力保留
FFmpeg
NOT AVAILABLE
Render Readiness
BLOCKED（素材 + 未 Lock + 无 FFmpeg）
MP4 / Playback
FAIL（未到 Render）
9. Recovery（本阶段加固）
能力	结果
Generation Retry（新 Task，不改写 FAILED）
PASS（既有）
Duplicate Generate 防护
PASS（IMAGE / TTS / STORYBOARD）
Partial Visual / Audio
服务层 per-target PASS；环境阻塞真实执行
Timeline Rebuild
PASS（既有）
Lock with missing assets
PASS（拒绝）
Render Retry
代码既有；未跑到 Runtime
10. Tests
项	结果
typecheck
PASS
lint
PASS
test
PASS（476 passed / 1 skipped）
integration / e2e
未单独跑全量 e2e；相关 unit/engine 已覆盖
11. Remaining Blockers（仅真实阻塞）
为项目配置支持 IMAGE 的 Provider / Model
配置 TTS Provider，并为角色设置 Voice ID
安装 FFmpeg + FFprobe（或设置 FFMPEG_PATH / FFPROBE_PATH）
装好后无需重做 Storyboard：从「生成缺失视觉」→「生成缺失对白」→ Rebuild / Lock Timeline → Render 即可。

Phase 15.8 执行结果（回顾）
目标：Script → Storyboard 可靠，并推进到 Assets。

已完成：

OpenAI-compatible JSON 抽取/修复、json_object、低 temperature、maxTokens
Storyboard 按 scene 生成 + 最多 3 次重试 + Schema Validation
Apply 写入 DRAFT，Confirm → READY
E01 真实 Runtime：Generate SUCCEEDED → Apply → Board READY（32 shots） → Stage VISUAL_ASSETS
15.8 结束时诚实阻塞（与 15.9 相同环境项）：IMAGE / TTS / FFmpeg 未就绪。

Phase 15.9 本阶段实际改动
assertNoActiveGeneration + IMAGE / TTS / STORYBOARD 防并发
Core：canLockTimeline 依赖素材完整；musicExpected 默认 0
Overview：musicExpected / sfxExpected = 0（MVP 不因 Music 误阻塞）
Timeline Lock：缺失 visual/dialogue → TIMELINE_INCOMPLETE
Assets 页：Missing Visual / Dialogue 可点进对应生产页
补充单测；全量 API test 通过
结论：生产链状态机与失败/锁定/并发防护已硬化；E01 无法在本机继续到 MP4，唯一原因是 IMAGE + TTS + FFmpeg 环境缺失，不是假完成。