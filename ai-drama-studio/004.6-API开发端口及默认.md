现在对项目进行一次「开发环境配置调整」。

当前项目状态：

- Phase 1 Monorepo 已完成
- Phase 2 Project Workspace 已完成
- Phase 3 World System 已完成
- Phase 4 AI Generation Engine v1 已完成
- Phase 4.5 AI Provider Management 已完成
- World CRUD 不允许修改
- Generation Engine 不允许破坏
- AI Provider Management 不允许破坏
- 当前 Provider Resolver 已实现：

Project Provider
    ↓
Default Provider
    ↓
System .env Provider
    ↓
NO_AI_PROVIDER_CONFIGURED

本次只修改：

1. Web 开发端口
2. API 开发端口
3. 所有相关 API Base URL / CORS / runtime config
4. System .env Provider 默认配置改为 DeepSeek

不要开发新功能。
不要进入 Phase 5。
不要修改数据库模型。
不要修改 Prisma migration。
不要修改 AI Provider 数据模型。
不要修改 GenerationTask。
不要修改 World CRUD。

==================================================
一、开发端口调整
==================================================

当前：

Web:
3000

API:
3001

这两个端口经常被占用。

请统一修改为：

Web:
3010

API:
3011

Mobile:
保持现有端口，不要修改，除非发现它明确依赖 Web/API 的旧端口。

Desktop:
保持现有端口，不要修改。

==================================================
二、全局检查端口引用
==================================================

不要只修改 Nuxt 和 NestJS 的启动端口。

请全局搜索：

3000
3001
localhost:3000
localhost:3001

检查：

- package.json
- apps/web
- apps/api
- packages/config
- packages/api-client
- .env
- .env.example
- Nuxt config
- NestJS bootstrap
- CORS
- runtimeConfig
- API Base URL
- Mobile
- Desktop
- Turbo scripts
- 测试配置
- Vitest
- Playwright（如果存在）
- README / 开发文档中涉及启动地址的地方

所有真正属于 Web/API 开发地址的引用都需要同步。

不要机械替换所有 3000 / 3001。

例如：

- 数据库端口 5432 不允许修改
- PostgreSQL 不允许修改
- Desktop 1420 不允许修改
- Mobile 3002 如果不是 Web/API 地址，不允许修改

==================================================
三、Web
==================================================

Web 开发服务器：

http://localhost:3010

检查 Nuxt 配置。

确保：

pnpm dev:web

启动后监听：

http://localhost:3010

如果当前 root package：

pnpm dev

会同时启动多个服务，也必须确保 Web 使用 3010。

==================================================
四、API
==================================================

API 开发服务器：

http://localhost:3011

确保：

pnpm dev:api

启动后监听：

http://localhost:3011

Health：

http://localhost:3011/health

应该返回：

{
  "status": "ok"
}

==================================================
五、API Client
==================================================

重点检查：

packages/api-client

当前 Web / Mobile / Desktop 都通过共享 API Client 调用 API。

不能继续默认：

http://localhost:3001

应该统一改成：

http://localhost:3011

如果 API Base URL 当前来自：

packages/config

则统一从 config 获取。

不要在 Web / Mobile / Desktop 中分别硬编码 API 地址。

==================================================
六、Web → API
==================================================

确保：

Web:
http://localhost:3010

API:
http://localhost:3011

Web 调用：

http://localhost:3011

检查：

- CORS
- API Client
- Nuxt runtime config
- 环境变量

确保：

http://localhost:3010

可以正常访问：

http://localhost:3011

==================================================
七、CORS
==================================================

NestJS API 的 CORS 必须允许：

http://localhost:3010

如果当前只允许：

http://localhost:3000

必须修改。

不要为了方便直接设置：

origin: "*"

保持当前项目安全设计。

==================================================
八、环境变量
==================================================

检查 .env.example。

修改：

WEB_PORT=3010
API_PORT=3011

如果当前项目没有 WEB_PORT / API_PORT，而是直接在配置中定义端口，请保持当前项目风格。

重要：

不要创建重复配置。

最终应该只有一个明确的开发环境配置来源。

==================================================
九、System AI Provider 改为 DeepSeek
==================================================

当前项目已经支持：

System .env Provider fallback。

现在将 System Provider 默认配置改成：

Provider:
OpenAI Compatible

DeepSeek Base URL:

https://api.deepseek.com

Model:

deepseek-chat

因此：

AI_PROVIDER=OPENAI_COMPATIBLE
AI_BASE_URL=https://api.deepseek.com
AI_MODEL=deepseek-chat
AI_API_KEY=

AI_API_KEY 必须继续由用户自己在 .env 中填写。

绝对不要把真实 API Key 写进：

- 源代码
- package.json
- .env.example
- Git
- 前端代码
- Nuxt runtimeConfig.public
- Dockerfile
- README

==================================================
十、DeepSeek API Endpoint
==================================================

当前 OpenAI-compatible Provider 使用：

POST {AI_BASE_URL}/chat/completions

请确认 DeepSeek 配置：

AI_BASE_URL=https://api.deepseek.com

最终请求：

https://api.deepseek.com/chat/completions

Authorization：

Bearer ${AI_API_KEY}

不要修改 Provider 架构。

==================================================
十一、.env.example
==================================================

将示例调整为：

AI_PROVIDER=OPENAI_COMPATIBLE
AI_BASE_URL=https://api.deepseek.com
AI_API_KEY=
AI_MODEL=deepseek-chat

同时保留：

AI_ENCRYPTION_KEY=

并给出必要说明：

AI_API_KEY 只允许存在 API 服务端。

==================================================
十二、Provider Resolver 行为
==================================================

不要改变现有优先级：

1. Project Provider
2. Default Provider
3. System .env Provider

本次只是将：

System .env Provider

默认配置改为 DeepSeek。

例如：

项目没有指定 Provider
        ↓
Default Provider 不存在
        ↓
System Provider
        ↓
DeepSeek

如果项目指定了自己的 Provider：

项目 Provider
        ↓
DeepSeek System Provider 不参与

==================================================
十三、不要创建数据库 DeepSeek Provider
==================================================

不要在 Prisma seed 中自动创建：

DeepSeek Provider

不要因为 System Provider 改成 DeepSeek 而创建 AiProvider 数据。

System Provider 仍然来自：

.env

数据库中的 AiProvider 仍然属于：

用户 / 项目配置体系。

==================================================
十四、AI Provider UI
==================================================

不要修改当前：

/ai-providers

不要增加 DeepSeek 固定卡片。

System Provider 是服务端 fallback。

如果现有 UI 会显示：

System Provider

确保它显示：

DeepSeek

OpenAI Compatible

deepseek-chat

但不要让用户误以为它是一个可以编辑 API Key 的数据库 Provider。

==================================================
十五、环境变量优先级
==================================================

明确保持：

数据库 Provider
>
.env System Provider

例如：

Project Provider = 用户自己的 OpenAI

那么：

世界观生成 → OpenAI

而不是：

世界观生成 → DeepSeek

只有没有项目 Provider、没有 Default Provider 时：

世界观生成 → DeepSeek System Provider

==================================================
十六、API Key 安全
==================================================

再次确认：

AI_API_KEY

只能由 NestJS 服务端读取。

不要：

- 暴露给 Web
- 暴露给 Mobile
- 暴露给 Desktop
- 返回 API
- 写日志
- 写错误信息

==================================================
十七、验证
==================================================

完成修改后：

1.

pnpm prisma validate

2.

pnpm lint

3.

pnpm typecheck

4.

pnpm test

全部必须通过。

==================================================
十八、启动验证
==================================================

启动 API：

pnpm dev:api

确认：

http://localhost:3011/health

返回：

{
  "status": "ok"
}

启动 Web：

pnpm dev:web

确认：

http://localhost:3010

可以正常打开。

==================================================
十九、API 连通性验证
==================================================

确认 Web：

http://localhost:3010

能够通过：

@ai-drama-studio/api-client

访问：

http://localhost:3011

不要出现：

CORS error

Connection refused

404

旧端口 3000 / 3001

==================================================
二十、DeepSeek System Provider 验证
==================================================

如果 AI_API_KEY 已配置：

进入：

项目
→ 星河碰撞
→ 世界观
→ AI生成世界观

如果：

Project Provider 未设置

且：

Default Provider 未设置

应该自动使用：

DeepSeek System Provider

生成任务应该记录：

provider = OPENAI_COMPATIBLE
model = deepseek-chat

不要记录 API Key。

==================================================
二十一、旧端口检查
==================================================

最终全局搜索：

3000
3001
localhost:3000
localhost:3001

输出仍然存在的引用。

如果存在：

判断是否属于：

1. 无关数字
2. 第三方配置
3. 历史文档
4. 测试快照

如果属于项目运行配置，必须修改。

==================================================
二十二、最终输出
==================================================

完成后请告诉我：

1. Web 新端口
2. API 新端口
3. 修改了哪些文件
4. 哪些地方引用了旧端口
5. API Client 是否同步
6. CORS 是否同步
7. System Provider 是否改为 DeepSeek
8. DeepSeek Base URL
9. DeepSeek Model
10. 是否修改数据库
11. 是否修改 Prisma migration
12. pnpm prisma validate
13. pnpm lint
14. pnpm typecheck
15. pnpm test
16. http://localhost:3011/health 是否正常
17. http://localhost:3010 是否正常

本阶段到此结束，不要继续开发其他功能。