继续执行 Phase 15.9.5。

Preflight Environment Audit 已完成。

当前真实结论：

1. IMAGE capability 已实现
2. TTS capability 已实现
3. Provider runtime 当前只有 OPENAI_COMPATIBLE
4. DeepSeek 只负责 CHAT / STRUCTURED_OUTPUT
5. 星河碰撞当前 IMAGE / TTS 未配置
6. E01 目前：
   Script READY
   Storyboard READY（32 shots）
   Visual 0/32
   Audio 0/19
   Timeline DRAFT
   Render BLOCKED
7. Character voiceId 当前为空
8. Windows 10
9. FFmpeg / FFprobe 未安装

现在进入 Bootstrap。

不要修改 Storyboard、Episode Workspace、Timeline、Render 等业务架构。

==================================================
第一步：FFmpeg
==================================================

先检查：

where.exe ffmpeg
where.exe ffprobe

如果不存在：

执行：

winget install Gyan.FFmpeg

安装完成后重新打开 shell。

验证：

ffmpeg -version
ffprobe -version

如果已经存在：

不要重复安装。

==================================================
第二步：Provider
==================================================

不要修改现有 DeepSeek Provider。

创建一个新的 OPENAI_COMPATIBLE Provider：

Provider：

OpenAI

Base URL：

https://api.openai.com/v1

API Key：

使用环境变量或安全输入方式。

绝对不要：

hardcode API key

绝对不要：

把 API key 写入 Git

绝对不要：

把 API key 输出到报告。

==================================================
第三步：IMAGE
==================================================

确认 OpenAI Provider 支持：

IMAGE

然后确认账号实际可用的 IMAGE model。

不要猜模型。

不要直接假设某个模型一定可用。

如果当前 API 可以查询模型：

优先查询。

然后创建：

IMAGE Model

并配置：

ProjectAiConfig

project：

demo-xinghe

capability：

IMAGE

provider：

OpenAI

model：

实际可用模型

==================================================
第四步：TTS
==================================================

确认 OpenAI Provider 支持：

TTS

确认账号实际可用的 TTS model。

不要猜。

配置：

ProjectAiConfig

project：

demo-xinghe

capability：

TTS

provider：

OpenAI

model：

实际可用模型

==================================================
第五步：不要自动配置 Character Voice
==================================================

先列出 E01 实际使用的 Character。

输出：

Character ID
Character Name
当前 voiceId
是否需要 TTS

不要自动随机分配 voice。

先告诉我需要配置哪些角色。

如果已有用户提供的 Voice ID：

才写入。

==================================================
第六步：Environment Check
==================================================

确认：

IMAGE = READY

TTS = READY

FFmpeg = READY

FFprobe = READY

Voice = READY / MISSING

不要修改 Episode Production Stage。

不要修改 Asset 状态。

==================================================
第七步：Visual Smoke Test
==================================================

只有 IMAGE READY 后：

进入：

E01
→ Assets
→ Shot 001

执行：

Generate Image

只生成 Shot 001。

不要 Generate All。

必须真实创建 GenerationTask。

验证：

QUEUED
→ RUNNING
→ SUCCEEDED

然后：

Preview

Apply

确认：

Asset READY

Shot → Asset relation 正确。

==================================================
第八步：TTS Smoke Test
==================================================

只有 TTS Provider + Voice READY 后：

选择 E01 第一个真实对白 ScriptBlock。

执行：

Generate TTS

必须真实：

ScriptBlock
→ Character
→ Voice ID
→ TTS Provider
→ Audio
→ Preview
→ Apply

验证：

Audio Asset READY

==================================================
第九步：Smoke Test 失败处理
==================================================

如果 IMAGE 失败：

不要修改代码制造成功。

记录：

Provider
Model
HTTP status
Error type
GenerationTask status

如果 TTS 失败：

同样处理。

不要进入全量生成。

==================================================
第十步：Smoke Test 成功后停止
==================================================

如果：

Shot 001 IMAGE 成功

AND

第一条 Dialogue TTS 成功

先不要生成全部 32 张。

输出：

# Phase 15.9.5 Bootstrap Smoke Test Result

包括：

FFmpeg
FFprobe
IMAGE Provider
IMAGE Model
TTS Provider
TTS Model
Characters requiring Voice
Shot 001 Image
First Dialogue TTS

以及：

下一步需要我确认什么。

==================================================
绝对禁止
==================================================

不要：

Mock
Fake Asset
Fake Audio
Fake MP4
修改 productionStage
直接写 READY
批量生成 32 shots
批量生成 19 audio
修改 Storyboard
修改 Timeline
修改 Render
删除 E01 数据
prisma migrate reset

当前阶段的目标只有：

真实 Provider + FFmpeg 环境准备
+
一个 Image Smoke Test
+
一个 TTS Smoke Test。