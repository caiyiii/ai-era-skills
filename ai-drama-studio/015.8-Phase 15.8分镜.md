# Phase 15.8 — Storyboard Reliability & Episode Production Continuation

你现在继续开发 AI-Drama-Studio。

本阶段基于当前真实代码继续开发。

不要假设项目处于 Phase 15.7 Prompt 中描述的旧状态。

必须先阅读当前代码，然后根据真实代码执行。

============================================================
一、本阶段真实当前状态
============================================================

当前项目已经真实完成：

1. Season Planning 与 Episode Workspace 路由已经修复。
2. Episode 父级页面已经改成路由容器。
3. Season Planning 已移动到：
   apps/web/pages/projects/[id]/seasons/[seasonId]/index.vue

4. Episode Workspace 已移动到：
   apps/web/pages/projects/[id]/seasons/[seasonId]/episodes/[episodeId]/index.vue

5. Episode Production 子页面已经存在：

/projects/:projectId/seasons/:seasonId/episodes/:episodeId/plan
/projects/:projectId/seasons/:seasonId/episodes/:episodeId/script
/projects/:projectId/seasons/:seasonId/episodes/:episodeId/storyboard
/projects/:projectId/seasons/:seasonId/episodes/:episodeId/assets
/projects/:projectId/seasons/:seasonId/episodes/:episodeId/timeline
/projects/:projectId/seasons/:seasonId/episodes/:episodeId/render

6. E01 已经真实完成：

Script Generate
→ Preview
→ Apply
→ Confirm

7. 当前 E01 的真实状态已经进入：

STORYBOARDING

8. E01 当前 Next Action：

GENERATE_STORYBOARD

9. Locations 已经是真实 CRUD + AI Generate，不再是占位页面。

10. 当前真正阻塞：

Storyboard AI Generation。

DeepSeek / deepseek-chat 当前真实调用仍可能返回非法 JSON，例如：

Expected ',' or ']' after array element in JSON

因此：

Visual Assets
Audio
Timeline
Render

目前不能宣称已经真实打通。

============================================================
二、本阶段唯一最终目标
============================================================

不要把目标定义成：

“修改了一些代码”

也不要定义成：

“测试通过”

本阶段真正目标：

用户打开：

http://localhost:3010/

进入：

星河碰撞
→ 第一季
→ E01 · 星系碰撞
→ 进入制作

然后：

当前阶段：分镜

点击：

AI 生成分镜

必须真实完成：

Generate
→ Provider
→ JSON Extraction
→ JSON Parse
→ Schema Validation
→ Preview
→ Apply
→ Confirm

成功之后：

Episode Production Stage

必须真实进入：

VISUAL_ASSETS

然后继续：

Storyboard
→ Visual Assets
→ Audio
→ Timeline
→ Render

如果某一步真实 Provider 不支持：

必须明确显示真实能力限制。

禁止 Mock 成功。

============================================================
三、绝对禁止事项
============================================================

本阶段禁止：

1. reset 数据库
2. 删除现有 E01
3. 删除现有 Script
4. 重新生成 E01 Script
5. 重新设计 Episode Workspace
6. 重新设计 Season Planning
7. 重新设计 Location
8. 新建第二套 Provider 架构
9. 新建第二套 GenerationTask
10. 新建第二套 Storyboard Model
11. 新建第二套 Asset Model
12. 用 Mock 数据伪造 AI 成功
13. 直接把 AI Response 写入正式 Storyboard
14. 通过危险字符串替换强行修复 JSON
15. 修改测试让测试通过但真实运行失败
16. 只跑 typecheck / lint / test 后宣布完成
17. 通过前端硬编码 productionStage
18. 在 Vue 页面中重新实现 Stage Resolver
19. 自动调用 AI
20. 为了演示而自动 Apply
21. 删除已有业务能力
22. 把 DeepSeek 写死在 Storyboard Service
23. 绕过 AiService / ProviderResolver
24. 绕过 GenerationTask
25. 将 API Key / encryptedApiKey 返回前端

============================================================
四、第一步：先进行真实代码审计
============================================================

修改代码之前，先检查以下文件：

apps/api/src/modules/generation/storyboard-generation.service.ts

apps/api/src/modules/generation/storyboard-generation.schema.ts

apps/api/src/modules/generation/storyboard/apply-storyboard-generation.ts

apps/api/src/modules/generation/prompts/storyboard-generation.prompt.ts

apps/api/src/modules/ai/providers/openai-compatible.provider.ts

apps/api/src/modules/generation/generation.executor.ts

apps/api/src/modules/ai/ai.service.ts

apps/api/src/modules/storyboard/

packages/core/src/episode-production.ts

packages/core/src/storyboard.ts

prisma/schema.prisma

以及当前 Episode Storyboard 页面。

必须回答：

A. 当前 Storyboard Generation 实际调用链是什么？

B. 当前 JSON parsing 在哪一层？

C. 当前 Schema Validation 在哪一层？

D. 当前 GenerationTask retry 能力是什么？

E. 当前 Provider 是否强制发送：

response_format: {
  type: "json_object"
}

F. 当前 DeepSeek Provider 配置是什么？

G. 当前 Storyboard Apply 是否幂等？

H. 当前 Storyboard Confirm 是在哪里完成？

I. 当前 Stage Resolver 如何判断：

STORYBOARDING
→ VISUAL_ASSETS

J. 当前 Visual Asset Generation 是否已经存在？

K. 当前 TTS 是否已经存在？

L. 当前 Timeline Builder 是否已经存在？

M. 当前 Render Service 是否已经存在？

先审计。

不要因为 Prompt 已经描述过而假设代码存在。

============================================================
五、第一核心任务：修复 Storyboard JSON Generation
============================================================

当前核心代码：

apps/api/src/modules/ai/providers/openai-compatible.provider.ts

其中已经存在：

generateStructured()

以及：

parseModelJson()

当前实现已经尝试：

1. 直接 JSON.parse
2. Markdown fence extraction
3. 提取最外层 {}

但是：

这还不够。

必须建立可靠链路：

AI Response
↓
Extract
↓
Parse
↓
Schema Validate
↓
Retry
↓
Success

============================================================
六、Provider 层职责
============================================================

优先在：

OpenAiCompatibleProvider

解决 Provider 层问题。

不要把 HTTP 调用重新写到 StoryboardGenerationService。

Provider 必须负责：

1. 调用 chat/completions
2. 判断 response 是否成功
3. 提取 message.content
4. 结构化 JSON extraction
5. JSON parse
6. 返回结构化对象

但是：

Provider 不应该知道 Storyboard 的业务 Schema。

因此：

Provider：

JSON Parse

StoryboardGenerationService：

Schema Validation

保持职责分离。

============================================================
七、response_format 兼容性
============================================================

当前代码可能对所有 Structured Output 强制：

response_format: {
  type: "json_object"
}

必须检查 DeepSeek / 当前实际模型是否真正支持该参数。

不要假设：

OpenAI Compatible
=
100% 支持 OpenAI response_format。

必须根据当前 Provider / Model 的真实行为决定。

如果当前模型使用：

response_format

会导致不稳定或不兼容：

允许对 OpenAI-compatible Provider 做兼容降级：

优先：

response_format json_object

失败后：

Prompt + JSON Extraction + Schema Validation

但不要吞掉真实错误。

日志必须能知道：

是否使用 response_format。

============================================================
八、JSON Extraction
============================================================

实现一个安全的 JSON extraction。

允许：

1. 直接解析完整 JSON。

2. 去除：

```json
...
Markdown fence。

去除 JSON 前后的自然语言。

例如：

Here is the result:

{
...
}

只提取：

{
...
}

如果最外层是 object：

找到真正的：

{
...
}

边界。

如果最外层是 array：

找到：

[
...
]

边界。

但是：

禁止：

通过大量 replace：

replace(/"/g, ...)
replace(/,/g, ...)
replace(/'/g, ...)

这种方式修改模型业务数据。

不要猜模型想表达什么。

============================================================
九、JSON Repair

允许有限的安全 Repair：

删除 Markdown fence
删除 JSON 前后的自然语言
删除明显 trailing comma

例如：

{
"shots": [
{...},
]
}

可以安全修复：

{
"shots": [
{...}
]
}

但是禁止：

模型缺少业务字段时自动猜值。

例如：

shotNumber:
"abc"

不能自动变成：

shotNumber:
1

必须 Schema Validation 失败。

============================================================
十、Schema Validation

当前项目已经存在：

apps/api/src/modules/generation/storyboard/storyboard-generation.schema.ts

并且已经存在：

STORYBOARD_GENERATION_JSON_SCHEMA

以及：

validateStoryboardGenerationResult()

必须复用。

不要重新创建第二套 Schema。

完整流程：

Provider
↓
parseModelJson()
↓
StoryboardGenerationService
↓
validateStoryboardGenerationResult()
↓
GenerationTask output

============================================================
十一、Retry

这是本阶段必须新增 / 完善的能力。

当前：

第一次生成失败

不能直接结束。

允许：

Initial Generation
+
最多 2 次 Retry

也就是：

最多 3 次 AI 请求。

流程：

Attempt 1
↓
JSON Parse / Schema Validation
↓
失败
↓
Retry 1
↓
失败
↓
Retry 2
↓
失败
↓
GenerationTask FAILED

不能无限循环。

============================================================
十二、Retry Prompt

Retry 不应该完全重复第一次 Prompt。

Retry 时必须明确告诉模型：

Your previous response was invalid.

Return the same requested storyboard data again.

Return ONLY valid JSON.

Do not include markdown.

Do not include explanations.

Make sure all objects and arrays are properly closed.

Make sure all strings are valid JSON strings.

Do not add fields outside the requested schema.

但是：

Retry 仍然必须使用：

当前 Episode
+
当前 Script
+
World
+
Characters
+
Locations
+
Story Context

不能把第一次结果当成新的业务事实。

============================================================
十三、Retry 失败错误

如果最终 3 次都失败：

GenerationTask：

FAILED

必须保留真实错误。

错误至少应该能够区分：

INVALID_JSON

SCHEMA_VALIDATION_FAILED

PROVIDER_ERROR

MODEL_NOT_FOUND

API_KEY_ERROR

TIMEOUT

等。

不要只返回：

“生成失败”。

============================================================
十四、日志

服务器日志必须记录：

GenerationTask ID

Provider

Model

Attempt

Response length

Response 前 N 个字符

Parse error

Schema error

Retry count

但是：

绝对不能记录：

API Key

encryptedApiKey

Authorization header

完整敏感请求。

============================================================
十五、Storyboard Prompt

修改：

apps/api/src/modules/generation/prompts/storyboard-generation.prompt.ts

但是：

不要无限增加 Prompt 长度。

当前 Prompt 已经存在大量 JSON 约束。

优化为：

System：

负责严格 JSON 输出规则。

User：

负责业务上下文。

System 必须明确：

Return ONLY valid JSON.

Do not use Markdown.

Do not use ```json.

Do not add explanations.

Do not add comments.

Do not add fields outside schema.

All strings must be valid JSON strings.

All arrays and objects must be properly closed.

No trailing commas.

============================================================
十六、不要使用“禁止字符串双引号”这种危险约束

当前 Prompt 中存在类似：

所有字符串里不要出现双引号字符 "

这种规则并不是可靠 JSON 策略。

不要依赖这种方式保证 JSON。

正确做法：

要求模型正常输出合法 JSON。

如果字符串需要双引号：

必须按照 JSON escape 规则处理。

例如：

He said "hello"

而不是要求：

字符串永远不能出现双引号。

============================================================
十七、Storyboard Schema 必须保持现有结构

当前项目已经有：

Storyboard

StoryboardShot

以及：

shotNumber
sceneNumber
scriptBlockIds
shotType
shotSize
cameraMovement
cameraAngle
composition
visualDescription
characterIds
location
action
dialogue
narration
direction
durationSeconds
transition
lighting
mood
visualStyle
imagePrompt
videoPrompt
negativePrompt
continuityNotes

必须继续复用。

不要新增重复字段。

============================================================
十八、Storyboard Context

Storyboard Generator 必须继续使用：

World
+
Story Bible
+
Characters
+
Locations
+
Season
+
Episode
+
Episode Plan
+
Confirmed Script

但必须控制 Context 大小。

不要：

把整个 Project Prisma JSON 全部发送给模型。

优先：

World summary
Story Bible summary
Relevant Characters
Relevant Locations
Episode outline
Episode continuity
Script scenes
Script blocks

============================================================
十九、Storyboard Location

当前数据库已经存在：

Location

并且：

Scene.locationId

已经存在。

StoryboardShot 当前仍有：

location String?

本阶段：

不要重新创建 Location。

如果当前 Storyboard 数据结构允许：

应尽量从：

Script Scene
→ Scene.locationId
→ Location

获取真实地点上下文。

Storyboard Shot 可以继续保持现有：

location

字段用于展示 / Prompt。

不要为了“规范化”而大规模改 Prisma。

只有当前生产链确实需要 relation 时才做最小增量。

============================================================
二十、Storyboard Apply

Apply 必须继续：

GenerationTask
→ validate
→ transaction
→ Storyboard
→ StoryboardShot

当前已经存在：

applyStoryboardGeneration()

必须复用。

Apply 后必须验证：

Storyboard exists

并且：

shots.length > 0

且：

所有 Shot：

shotNumber 合法
sceneNumber 合法
scriptBlockIds 合法
characterIds 合法
durationSeconds > 0

不能出现：

Storyboard exists

但是：

shots = []

============================================================
二十一、Apply 幂等

当前：

GenerationTask

已经有：

appliedAt

继续复用。

同一个 GenerationTask：

只能 Apply 一次。

第二次：

返回：

GENERATION_ALREADY_APPLIED

或者当前项目对应的已有错误。

不能创建第二个 Storyboard。

============================================================
二十二、Storyboard Confirm

检查当前项目真实 Confirm 实现。

必须保证：

Apply

不等于：

Confirm。

正确流程：

Generate
↓
Preview
↓
Apply
↓
Edit
↓
Confirm

只有 Confirm 后：

Episode Production Stage

才应该推进：

VISUAL_ASSETS

如果当前 Core Resolver 已经这么做：

继续复用。

不要在 Vue 中手工修改 Stage。

============================================================
二十三、E01 必须从当前状态继续

绝对不要：

重新生成 Script。

绝对不要：

删除 E01。

必须直接：

E01
→ 当前已确认 Script
→ AI 生成 Storyboard

如果当前数据库中 E01 Storyboard 有失败记录：

可以继续创建新的 GenerationTask。

不要删除历史 GenerationTask。

============================================================
二十四、Storyboard 页面

检查：

apps/web/pages/projects/[id]/seasons/[seasonId]/episodes/[episodeId]/storyboard.vue

必须确保：

页面标题：

E01 · 星系碰撞

分镜

Breadcrumb：

项目 / 星河碰撞 / 第一季 / E01 · 星系碰撞 / 分镜

顶部必须有：

返回本集工作台

AI 生成分镜

确认分镜

具体按钮状态必须根据真实 Storyboard 状态决定。

如果 Script 未确认：

禁止生成。

如果 Script 已确认：

允许生成。

如果 GenerationTask FAILED：

显示真实失败原因。

============================================================
二十五、Generation Preview

Storyboard Generate 成功：

不能自动 Apply。

必须返回：

GenerationTask

并允许：

Preview

用户确认后：

Apply

继续复用现有 GenerationTask / Preview / Apply 体系。

============================================================
二十六、Storyboard 成功后继续 Visual Assets

当 Storyboard Confirm 成功：

Episode Workspace：

productionStage

应该由：

resolveEpisodeProductionStage()

自动计算为：

VISUAL_ASSETS

NextAction：

GENERATE_MISSING_VISUAL_ASSETS

不要在 Vue 中：

stage = "VISUAL_ASSETS"

============================================================
二十七、Visual Assets 先做最小真实闭环

检查现有：

apps/api/src/modules/generation/image-generation.service.ts

以及：

Storyboard Shot
Image Generation
Asset

当前 Image Generation 已经存在。

不要重写。

本阶段只需要把：

Storyboard Shot
→ Image Generation
→ Preview
→ Apply

真正接到 Episode Assets 页面。

============================================================
二十八、Visual Asset Generation

用户点击：

AI 生成图片

必须：

Shot
+
World Context
+
Character Context
+
Location Context
+
Storyboard imagePrompt

形成最终 Prompt。

不要要求用户重新填写完整 Prompt。

继续使用：

ProjectAiConfig
ProviderResolver
AiCapability.IMAGE

禁止：

直接读取 API Key。

禁止：

写死 DeepSeek。

============================================================
二十九、Provider 不支持 Image

如果当前 Provider：

不支持 IMAGE

必须显示：

当前 Provider 不支持图片生成。

不能：

显示生成成功。

不能：

创建假的 Asset。

允许：

上传已有图片

或者：

选择已有图片

如果现有 Asset API 已支持：

直接复用。

============================================================
三十、Visual Preview → Apply

必须保持：

Generate
↓
Preview
↓
Apply

不能：

Generate
↓
直接创建正式 Asset

Apply 必须幂等。

============================================================
三十一、Visual Readiness

Episode Workspace / Assets 页面显示：

视觉素材：

X / Y

例如：

视觉素材：

1 / 5

并列出：

缺失：

Shot 002
Shot 003
Shot 004
Shot 005

不要只显示：

处理中。

============================================================
三十二、Partial Production

必须支持：

5 个 Shot

已经完成：

2 个

剩余：

3 个

用户可以：

只生成缺失 Shot。

不能重新生成整个 Storyboard。

============================================================
三十三、Audio

检查当前：

apps/api/src/modules/generation/tts-generation.service.ts

以及：

apps/api/src/modules/generation/episode-audio-generation.service.ts

当前项目已经存在：

TTS

GenerationTask

Apply

Asset

因此：

不要重新实现 TTS。

只需要确保 Episode Assets 能够使用现有能力。

============================================================
三十四、Dialogue Audio

对白来源：

ScriptBlock

必须：

ScriptBlock
→ TTS
→ Preview
→ Apply
→ Audio Asset

必须继续使用：

Character voiceProfile

如果需要 Voice ID：

使用当前项目现有配置。

如果没有：

明确提示用户。

不要假装生成。

============================================================
三十五、Audio Capability

必须继续使用：

AiCapability.TTS

AiCapability.MUSIC

AiCapability.SFX

如果 Provider 不支持：

明确显示。

不要伪造。

============================================================
三十六、Timeline

检查：

TimelineBuilderService

TimelineTimingService

TimelineContinuityService

CompositionService

以及：

prisma/schema.prisma

当前已经存在：

EpisodeTimeline

TimelineTrack

TimelineClip

TimelineStatus

TimelineTrackType

TimelineClipType

TimelineClipSourceType

不要重写 Timeline 架构。

============================================================
三十七、Timeline Draft

Storyboard
+
Visual Assets
+
Audio

满足当前 Core 的真实条件后：

允许建立：

DRAFT Timeline

不要自动 LOCK。

如果 Timeline 已存在：

不要重复创建。

============================================================
三十八、Timeline Clip 来源

至少支持：

Storyboard Shot
→ IMAGE / VIDEO

ScriptBlock
→ DIALOGUE

Episode Audio
→ MUSIC / SFX

如果没有 Video：

允许：

Image + duration

作为 Timeline Video / Image 内容。

不要因为没有 AI Video Provider 而阻塞 MVP Timeline。

============================================================
三十九、Timeline Duration

必须根据：

StoryboardShot.durationSeconds

计算 Clip 时间。

例如：

Shot 1 = 4s
Shot 2 = 5s
Shot 3 = 3s

Timeline：

0 → 4
4 → 9
9 → 12

禁止：

所有 Clip 都从 0 开始。

============================================================
四十、Timeline Missing Assets

如果：

Shot 005 缺少 Image

或者：

ScriptBlock 007 缺少 Dialogue Audio

Timeline 页面必须明确显示：

无法完整构建。

缺少：

Shot 005 Image

ScriptBlock 007 Dialogue Audio

不能假装：

Timeline Ready。

============================================================
四十一、Timeline Lock

只有：

Timeline 完整

且：

Timeline 非 STALE

才允许：

LOCK TIMELINE

Lock 后：

Render 才允许。

============================================================
四十二、Stale

继续使用项目现有 Stale 规则。

不要自己发明新的 Stale 机制。

至少确保：

Script change
→ Storyboard STALE

Storyboard change
→ Timeline STALE

Timeline STALE
→ Render blocked

不要自动删除旧数据。

============================================================
四十三、Render

检查当前 Phase 14：

RenderService
RenderJob
RenderArtifact
Manifest Snapshot
Worker
FFmpeg

不要重新设计。

Render 前必须检查：

Timeline exists
Timeline LOCKED
Timeline not STALE
Required visual assets complete
Required dialogue complete

Music / SFX：

只有 Episode / Timeline 真正要求时才检查。

不要因为：

Music = 0

就错误阻塞 MVP Render。

============================================================
四十四、FFmpeg

实际检查当前开发环境：

ffmpeg

ffprobe

如果不存在：

不要伪造 MP4。

必须明确：

FFmpeg 未安装。

但是：

不要因为 FFmpeg 不存在而停止：

Storyboard
Visual
Audio
Timeline

这些阶段。

============================================================
四十五、Render MVP

第一版只要求：

Image / Video
+
Dialogue

能够输出：

MP4

Music / SFX 如果没有：

不应该阻塞最小 MVP。

如果只有 Image：

允许：

Image + duration

通过 FFmpeg 生成静态画面视频。

============================================================
四十六、Episode Workspace

确保：

Episode Workspace

使用：

resolveEpisodeProductionStage()

resolveEpisodeNextAction()

resolveEpisodeProductionProgress()

resolveEpisodeProductionChecklist()

resolveEpisodeReadiness()

不要重新写状态判断。

当前真实状态：

E01：

Script confirmed

Storyboard not confirmed

因此应该显示：

当前阶段：

分镜

当前任务：

AI 生成分镜

主按钮：

AI 生成分镜

============================================================
四十七、页面路由

所有生产页面必须继续保持：

projectId
seasonId
episodeId

不要使用：

全局 selectedEpisode

不要依赖：

localStorage

不要丢：

episodeId

子页面：

/script
/storyboard
/assets
/timeline
/render

都必须属于：

当前 Episode。

============================================================
四十八、不要重新处理 Routing

Phase 15.7 已经解决：

Season Planning

和：

Episode Workspace

路由容器问题。

本阶段：

不要重新重构：

seasons/[seasonId].vue

episodes/[episodeId].vue

除非真实代码审计发现阻塞当前生产链的问题。

不要把已经解决的 Routing 当成主要工作。

============================================================
四十九、真实 Runtime 验收

完成代码后：

不要只运行：

pnpm typecheck

pnpm lint

pnpm test

这些只是基础检查。

必须启动：

API

Web

使用真实数据库。

然后：

打开：

http://localhost:3010/

进入：

星河碰撞

第一季
E01 · 星系碰撞
进入制作
确认当前：

STORYBOARDING

点击：

AI 生成分镜

检查：

GenerationTask

检查真实 Provider：

DeepSeek / 当前项目实际配置

检查：

Response

检查：

JSON extraction

检查：

JSON parse

检查：

Schema validation

成功后：

Storyboard Preview

点击：

Apply

检查数据库：

Storyboard exists

Shot count > 0

点击：

确认分镜

返回：

Episode Workspace

检查：

Stage = VISUAL_ASSETS

检查：

NextAction = GENERATE_MISSING_VISUAL_ASSETS

============================================================
五十、如果 Storyboard 仍然失败

不能停在：

“DeepSeek 返回非法 JSON”。

必须继续定位：

A. 实际 Request

B. 实际 Model

C. 实际 response_format

D. 实际 Response

E. parseModelJson()

F. JSON extraction

G. JSON.parse

H. validateStoryboardGenerationResult()

I. Retry

J. GenerationTask

找到真正失败层级。

============================================================
五十一、真实错误必须透明

如果：

Provider 返回非法 JSON：

显示：

Provider 返回非法 JSON。

如果：

Schema Validation 失败：

显示：

Storyboard Schema Validation 失败。

如果：

Provider 不支持 Structured Output：

显示：

当前 Provider 不支持结构化输出。

如果：

API Key 错误：

显示：

AI API Key 无效或没有权限。

不要统一显示：

生成失败。

============================================================
五十二、最终最低验收标准

本阶段至少必须真实完成：

E01

Script：

CONFIRMED

↓

Storyboard：

GENERATED

↓

Storyboard：

APPLIED

↓

Storyboard：

CONFIRMED

↓

Episode Stage：

VISUAL_ASSETS

↓

至少一个 Shot：

IMAGE GENERATED

↓

Preview

↓

Apply

↓

至少一个 Dialogue：

TTS Generated

↓

Preview

↓

Apply

↓

Timeline：

DRAFT

↓

Timeline：

LOCKED

如果：

FFmpeg 可用：

Render

↓

MP4

如果：

FFmpeg 不可用：

必须明确：

Render blocked

原因：

FFmpeg 未安装

但不能伪造 MP4。

============================================================
五十三、本阶段完成报告

完成后不要只回复：

“完成”。

必须输出：

Phase 15.8 Complete Report
1. Storyboard

Provider：

Model：

response_format：

JSON Extraction：

JSON Parse：

Schema Validation：

Retry：

Attempt Count：

Preview：

Apply：

Confirm：

Shot Count：

E01：

PASS / FAIL

2. Visual Assets

Image Provider：

Model：

Shot：

Generate：

Preview：

Apply：

Readiness：

E01：

PASS / FAIL

3. Audio

TTS Provider：

Model：

Dialogue：

Generate：

Preview：

Apply：

Readiness：

E01：

PASS / FAIL

4. Timeline

Timeline：

DRAFT：

Tracks：

Clips：

Duration：

Missing Assets：

LOCK：

STALE：

E01：

PASS / FAIL

5. Render

FFmpeg：

FFprobe：

RenderService：

RenderJob：

Manifest：

MP4：

E01：

PASS / FAIL

6. E01 当前真实状态

Production Stage：

Next Action：

Progress：

Checklist：

Readiness：

7. Remaining Blockers

这里只允许写：

真实阻塞项。

不要写：

未来可以增加字幕

未来可以增加转场

未来可以增加专业剪辑

这类非阻塞事项。

============================================================
五十四、最重要原则

本阶段唯一完成标准：

不是：

“代码写完”。

不是：

“测试通过”。

不是：

“页面存在”。

而是：

用户可以真正操作：

E01

↓

AI 生成分镜

↓

Preview

↓

Apply

↓

Confirm

↓

视觉素材

↓

音频

↓

Timeline

↓

Lock

↓

Render

↓

MP4

每一步：

必须真实发生业务动作。

每一步：

必须使用真实数据库状态。

每一步：

必须保持：

projectId
seasonId
episodeId

上下文。

如果真实 Provider 不支持某个能力：

必须诚实显示。

如果环境不支持 FFmpeg：

必须诚实显示。

如果 AI 返回非法 JSON：

必须：

Retry / 修复 / 明确失败。

绝对不能：

Mock。

绝对不能：

伪造成功。

绝对不能：

跳过失败步骤。

============================================================
五十五、执行顺序

严格按照：

Phase A
代码审计

↓

Phase B
Storyboard JSON Reliability

↓

Phase C
E01 Storyboard Runtime

↓

Phase D
Visual Assets Runtime

↓

Phase E
Audio Runtime

↓

Phase F
Timeline Runtime

↓

Phase G
Render Runtime

↓

Phase H
E01 Full Runtime Verification

不要一开始就大规模修改所有模块。

如果 Storyboard 仍然失败：

先把 Storyboard 真正修通。

不要提前重构 Timeline。

如果 Storyboard 成功：

继续 Visual。

如果 Visual 被 Provider 能力阻塞：

实现真实的 Upload / Existing Asset fallback（仅复用现有 Asset 能力），然后继续 Timeline。

如果 TTS 被 Provider 能力阻塞：

使用真实 Upload / Existing Audio Asset 能力继续。

最终目标：

让 AI-Drama-Studio 从：

“已经有很多生产页面”

真正变成：

“至少可以真实生产一集 Episode”。

现在开始。