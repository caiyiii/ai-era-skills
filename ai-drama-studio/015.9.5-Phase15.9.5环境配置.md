# Phase 15.9.5 — Production Environment Bootstrap & Runtime Verification

你现在继续开发 AI-Drama-Studio。

当前 Phase 15.8 和 Phase 15.9 已经完成。

本阶段不是继续扩展产品功能。

本阶段唯一目标：

# 补齐本地 AI Production Environment，并真实验证 E01 当前已经完成的生产链。

============================================================
一、当前真实项目状态
==========

根据 Phase 15.8 / 15.9 的真实 Runtime 结果：

Project：

星河碰撞

Season：

第一季

Episode：

E01 · 星系碰撞

当前：

Script
READY

Storyboard
READY

Storyboard Shots
32

Production Stage：

VISUAL_ASSETS

Next Action：

GENERATE_MISSING_VISUAL_ASSETS

Visual：

0 / 32

Dialogue Audio：

0 / 19

Timeline：

DRAFT

Timeline Lock：

BLOCKED

Render：

BLOCKED

当前真正阻塞：

1. IMAGE Provider 未配置
2. TTS Provider 未配置
3. Character Voice 未配置
4. FFmpeg / FFprobe 未安装

这些不是代码失败。

不要重新实现已经完成的生产链。

============================================================
二、本阶段禁止事项
=========

禁止：

1. 修改 Storyboard 架构
2. 重新生成 Script
3. 重新生成 Storyboard
4. 修改 Episode Routing
5. 修改 Episode Workspace 架构
6. 修改 Timeline Schema
7. 重写 RenderService
8. 创建假的 Image Asset
9. 创建假的 Audio Asset
10. 创建假的 MP4
11. Mock IMAGE Provider
12. Mock TTS Provider
13. 绕过 Project AI Config
14. 把 API Key 写死到代码
15. 把 API Key 返回前端
16. 自动修改数据库制造 READY 状态
17. 删除当前 E01 数据
18. prisma migrate reset
19. seed 一个新的测试 Episode
20. 为了测试而修改 Core Resolver

本阶段只做：

Environment Bootstrap
+
Provider Configuration
+
Voice Configuration
+
FFmpeg Installation / Detection
+
Runtime Verification

============================================================
三、第一步：完整审计当前 AI Provider 架构
===========================

先不要修改代码。

检查：

apps/api/src/modules/ai/

以及：

Provider
Model
Capability
ProjectAiConfig
ProviderResolver
AiService

重点检查：

AiCapability

必须确认当前是否已有：

CHAT
STRUCTURED_OUTPUT
IMAGE
VIDEO
TTS
MUSIC
SFX

如果 Enum 已存在：

不要修改。

============================================================
四、检查当前 Provider
===============

列出当前代码中已经实现的 Provider。

例如：

OpenAI Compatible
DeepSeek
OpenAI
其他

必须以代码为准。

输出：

Provider：

Capability：

Model：

Status：

例如：

Provider A
CHAT ✓
STRUCTURED_OUTPUT ✓
IMAGE ✗
TTS ✗

Provider B
IMAGE ✓
TTS ✓

不要猜。

============================================================
五、检查 IMAGE Provider
===================

搜索：

AiCapability.IMAGE

检查：

1. 是否已经有 Image Provider
2. 是否已经有 ImageGenerationService
3. ProviderResolver 是否可以 resolve IMAGE
4. ProjectAiConfig 是否可以配置 IMAGE
5. Model 是否需要单独配置
6. API Key 环境变量是什么
7. 当前项目是否已有 IMAGE Model

必须输出：

IMAGE Capability 当前状态。

如果代码已经支持：

不要重新写。

============================================================
六、检查 TTS Provider
=================

搜索：

AiCapability.TTS

检查：

1. TTS Provider
2. TTS Generation Service
3. ProviderResolver
4. ProjectAiConfig
5. Model
6. API Key
7. Voice ID

输出：

TTS Capability 当前状态。

============================================================
七、检查 Character Voice
====================

检查：

Character Model

以及：

voiceProfile

或者当前项目实际使用的字段。

必须确认：

Character：

是否有 Voice ID

如果没有：

不要自动生成假的 Voice ID。

明确：

需要用户配置。

============================================================
八、检查当前环境变量
==========

检查：

.env

.env.example

apps/api/.env

apps/web/.env

以及项目实际读取的 Config。

但是：

绝对不要在最终报告中输出：

真实 API Key。

只允许输出：

已配置 / 未配置。

例如：

IMAGE_API_KEY:
CONFIGURED

TTS_API_KEY:
MISSING

不要输出：

sk-xxxx

============================================================
九、不要自行选择第三方 Provider
====================

如果项目当前没有 IMAGE Provider：

不要擅自安装一个新的 SDK。

先输出：

当前项目已经支持：

...

当前项目没有支持：

...

然后根据当前项目架构选择最小改动方案。

如果必须新增 Provider：

必须说明：

Provider Name
SDK
Capability
Required ENV
Required Model

但是：

只有确实缺失时才增加。

============================================================
十、OpenAI-compatible Provider
============================

当前项目已经存在：

OpenAiCompatibleProvider

不要因为 IMAGE / TTS 缺失而把：

IMAGE

TTS

全部强行塞进：

OpenAiCompatibleProvider。

只有当真实 API：

支持对应 capability

才实现。

否则：

保持：

CAPABILITY_NOT_IMPLEMENTED

============================================================
十一、IMAGE Provider 配置
====================

如果当前项目已有 IMAGE Provider：

只完成：

Provider Config
+
Model Config
+
Project AI Config

不要改业务代码。

检查：

ProjectAiConfig

是否已经支持：

capability = IMAGE

并配置：

provider
+
model

============================================================
十二、TTS Provider 配置
==================

同样：

如果已有 TTS Provider：

配置：

provider
+
model

并配置：

Character Voice ID。

不要修改 Script。

不要重新生成 Script。

============================================================
十三、Project AI Config
====================

确认：

项目：

星河碰撞

存在：

IMAGE

配置。

并且：

TTS

配置。

不要把 Provider 配置写死。

必须继续使用：

ProjectAiConfig

============================================================
十四、FFmpeg
=========

检查：

ffmpeg

ffprobe

执行：

which ffmpeg

which ffprobe

以及：

ffmpeg -version

ffprobe -version

如果已经存在：

记录：

路径

版本

如果不存在：

不要修改 RenderService。

输出：

FFmpeg NOT INSTALLED

============================================================
十五、FFmpeg 安装
============

如果当前开发环境允许安装：

使用当前系统推荐方式安装。

如果当前环境是 macOS + Homebrew：

优先：

brew install ffmpeg

如果当前环境不是 macOS：

先识别操作系统。

不要执行错误平台的安装命令。

安装完成后重新检查：

ffmpeg -version

ffprobe -version

============================================================
十六、FFmpeg PATH
==============

检查项目当前：

FFMPEG_PATH

FFPROBE_PATH

配置方式。

如果系统 PATH 已经存在：

优先使用系统 PATH。

如果项目要求：

FFMPEG_PATH

FFPROBE_PATH

则：

配置真实路径。

不要硬编码开发机绝对路径到 Git。

============================================================
十七、Environment Verification Endpoint
====================================

检查当前项目是否已经有：

AI capability status

health

environment check

如果已有：

复用。

如果完全没有：

可以增加一个开发环境诊断 API。

例如：

GET /health/production

或者项目已有等价 endpoint。

必须只返回：

Provider availability
Capability availability
FFmpeg availability

绝对不能返回：

API Key。

============================================================
十八、Production Environment Check
===============================

必须能够回答：

IMAGE：

READY / NOT READY

TTS：

READY / NOT READY

VOICE：

READY / NOT READY

FFmpeg：

READY / NOT READY

FFprobe：

READY / NOT READY

============================================================
十九、不要自动生成 E01 Asset
===================

环境配置完成以后：

才开始真实 Runtime。

第一步：

不要点击 Generate All。

先只生成：

一个 Shot。

例如：

Shot 001

============================================================
二十、Visual Runtime Smoke Test
============================

真实操作：

E01

→ Assets

→ Shot 001

→ Generate Image

必须真实：

GenerationTask

→ Provider

→ Model

→ Image

→ Preview

→ Apply

成功后：

Shot 001

Visual Asset：

READY

============================================================
二十一、Visual Apply 验证
===================

Apply 后检查数据库：

Asset exists

Asset source

Asset status

Shot relation

GenerationTask

AppliedAt

必须确认：

不会生成重复 Asset。

============================================================
二十二、如果 IMAGE 失败
===============

不要修改代码制造成功。

记录：

Provider

Model

Request

Response

Error

GenerationTask

状态。

判断：

Configuration Error

Provider Error

Model Error

API Error

Timeout

Quota

等。

然后停止 Visual 全量生成。

============================================================
二十三、Visual 全量生成
===============

只有：

Shot 001 Smoke Test

成功后：

执行：

Generate Missing Visual Assets

当前：

32 Shots

目标：

32 / 32

但是：

允许部分失败。

如果：

30 / 32

必须保留：

30 个成功。

只 Retry：

2 个失败。

============================================================
二十四、TTS Smoke Test
==================

Visual Smoke Test 成功后：

选择：

第一个存在对白的 ScriptBlock。

执行：

Generate TTS

必须真实：

ScriptBlock

→ Character

→ Voice ID

→ TTS Provider

→ Audio

→ Preview

→ Apply

============================================================
二十五、Character Voice
===================

如果 TTS 提示：

Voice ID missing

不要创建假的。

检查当前项目 Characters。

为：

E01 使用到的角色

配置真实 Voice ID。

Voice ID 必须来自当前 TTS Provider 支持的 Voice。

如果当前 Provider 有 Voice List API：

优先使用。

如果没有：

根据 Provider 官方支持的 Voice ID 配置。

============================================================
二十六、TTS 全量
==========

Smoke Test 成功后：

生成：

Generate Missing Dialogue Audio

目标：

19 / 19

允许部分失败。

失败项：

可以单独 Retry。

============================================================
二十七、Timeline Rebuild
====================

Visual / Audio 完成后：

回到：

Timeline

执行：

Rebuild Timeline

必须确认：

仍然只有：

1 EpisodeTimeline

不是创建第二个。

检查：

Tracks

Clips

Duration

Source Asset

Source Audio

============================================================
二十八、Timeline Validation
=======================

必须验证：

1. 所有必要 Shot 都有 Visual Asset
2. 所有必要 Dialogue 都有 Audio
3. Timeline duration 合法
4. Clip start/end 合法
5. 没有 unintended overlap
6. Timeline 状态：

DRAFT

============================================================
二十九、Timeline Lock
=================

点击：

Lock Timeline

必须成功。

检查：

Timeline status：

LOCKED

Episode Resolver：

READY_TO_RENDER

如果不能 Lock：

不要强行修改数据库。

记录：

真实 blocker。

============================================================
三十、Render Readiness
===================

打开：

Render

页面。

必须看到：

✓ Script
✓ Storyboard
✓ Visual
✓ Audio
✓ Timeline
✓ Timeline Locked
✓ FFmpeg
✓ FFprobe

状态：

READY TO RENDER

============================================================
三十一、Render Smoke Test
=====================

执行：

Render Episode

必须创建：

RenderJob

状态：

QUEUED

→ RUNNING

→ SUCCEEDED

如果项目当前使用 Worker：

等待 Worker。

不要：

setTimeout 假装完成。

============================================================
三十二、Render Artifact
===================

Render 成功后：

检查：

RenderArtifact

必须存在。

检查：

File exists

File size > 0

Duration > 0

Mime type：

video/mp4

============================================================
三十三、MP4 Playback
================

从 Web Render 页面：

播放：

E01 MP4。

必须真实播放。

不能：

只检查数据库。

============================================================
三十四、如果 Render 失败
================

记录：

RenderJob

FFmpeg command

Exit code

stderr

stdout

但是：

不要输出敏感信息。

根据真实错误修复。

如果是：

FFmpeg PATH

修复环境。

如果是：

输入素材路径

修复 RenderService。

如果是：

Audio codec

修复 FFmpeg 参数。

不要创建假 MP4。

============================================================
三十五、不要修改生产数据制造完成
================

绝对禁止：

UPDATE Episode SET productionStage = COMPLETED

或者：

手工修改：

Timeline = LOCKED

Render = SUCCEEDED

Asset = READY

除非这些状态是由真实业务流程产生。

============================================================
三十六、最终 Runtime 验收
=================

目标：

E01

Script
✓

Storyboard
✓ 32 shots

Visual
✓ 32 / 32

Audio
✓ 19 / 19

Timeline
✓ DRAFT

Timeline
✓ LOCKED

Render
✓ SUCCEEDED

MP4
✓ PLAYABLE

Episode Production Stage：

COMPLETED

============================================================
三十七、如果无法完成
==========

必须明确：

Code Blocker

还是：

Environment Blocker

还是：

Provider Blocker

还是：

Quota / Account Blocker

不要把环境问题写成代码问题。

============================================================
三十八、测试
======

完成环境配置后运行：

pnpm typecheck

pnpm lint

pnpm test

并运行：

相关 integration tests

相关 e2e tests

如果项目已有：

production pipeline test

也运行。

============================================================
三十九、最终报告
========

完成后输出：

# Phase 15.9.5 Complete Report

## 1. Environment

OS：

Node：

pnpm：

PostgreSQL：

Docker：

FFmpeg：

FFprobe：

## 2. AI Providers

IMAGE Provider：

Model：

Status：

TTS Provider：

Model：

Status：

## 3. Project AI Config

IMAGE：

CONFIGURED / NOT CONFIGURED

TTS：

CONFIGURED / NOT CONFIGURED

## 4. Character Voice

Characters Used：

Configured：

Missing：

## 5. E01 Visual

Total Shots：

Ready：

Failed：

Skipped：

## 6. E01 Audio

Dialogue Blocks：

Ready：

Failed：

Missing Voice：

## 7. Timeline

Status：

Tracks：

Clips：

Duration：

Lock：

## 8. Render

Readiness：

RenderJob：

RenderArtifact：

MP4：

Playback：

## 9. Tests

Typecheck：

Lint：

Test：

Integration：

E2E：

## 10. Remaining Blockers

只写真实 Blocker。

## 11. 最终结论

必须明确：

A. E01 已成功生成 MP4

或者：

B. E01 已完成到 Timeline，Render 被环境阻塞

或者：

C. E01 被 Provider / Account / Quota 阻塞

或者：

D. E01 存在真实代码 Bug

不得模糊表述。

============================================================
四十、最重要原则
========

本阶段不是：

“开发更多功能”。

而是：

“让已经开发好的功能真正跑起来”。

不要为了完成报告而修改数据。

不要 Mock。

不要假成功。

不要跳过：

Generate
Preview
Apply
Confirm
Lock
Render

每一步都必须真实发生。

现在开始：

第一步只做：

Phase 15.9.5 Preflight Environment Audit

先告诉我：

1. 当前 IMAGE Provider 支持情况
2. 当前 TTS Provider 支持情况
3. 当前 Character Voice 配置方式
4. 当前 FFmpeg / FFprobe 状态
5. 当前需要配置哪些环境变量
6. 当前 Project AI Config 应该如何配置

完成环境审计后，再继续 Runtime Smoke Test。





Phase 15.9.5 Preflight Environment Audit
结论先说： 代码侧 IMAGE / TTS 能力已实现；当前阻塞全是 Provider 配置 + Character Voice + FFmpeg 环境，不是生产链缺功能。DeepSeek 只能做文本，不能当 IMAGE/TTS。

1. IMAGE Provider 支持情况
项	状态
AiCapability.IMAGE
已存在（Prisma / types）
ImageGenerationService
已存在
OpenAiCompatibleProvider.generateImage
已实现 → POST {baseUrl}/images/generations
ProviderResolver resolve IMAGE
支持（须 ProjectAiConfig，不用 .env system fallback）
Project AI Config IMAGE
可配置；星河碰撞当前 NOT CONFIGURED
implemented（能力清单）
true
当前库内 Provider（星河碰撞在用）：

Provider	Kind	BaseUrl	Capability
我的DS-API
OPENAI_COMPATIBLE
https://api.deepseek.com
CHAT ✓ · STRUCTURED_OUTPUT ✓ · IMAGE ✗ · TTS ✗
DeepSeek 文本 API 没有 /images/generations。把 IMAGE 绑到同一 Provider 会得到真实上游失败 / CAPABILITY_NOT_SUPPORTED，不是配置成功。

需要另建 IMAGE Provider（仍用现有 OPENAI_COMPATIBLE，不要新 SDK）：

BaseUrl：真实支持 Images 的网关（例如 OpenAI https://api.openai.com/v1）
Capabilities：含 IMAGE
Model：该网关支持的图像模型（如 dall-e-3 / gpt-image-1，以你账号为准）
API Key：写入 Provider（加密存库），不要写死进代码
2. TTS Provider 支持情况
项	状态
AiCapability.TTS
已存在
TtsGenerationService
已存在
OpenAiCompatibleTtsAdapter
已实现 → POST {baseUrl}/audio/speech
Project AI Config TTS
可配置；当前 NOT CONFIGURED
System .env fallback
仅 CHAT / STRUCTURED_OUTPUT，不覆盖 TTS
同样：DeepSeek 不能当 TTS。需要支持 /audio/speech 的 OPENAI_COMPATIBLE Provider + 模型（如 tts-1 / tts-1-hd）。

3. Character Voice 配置方式
字段：Character.voiceProfile（JSON）
TTS 读取：voiceProfile.voiceId（可被请求覆盖；空则 TTS_VOICE_REQUIRED）
UI：角色详情页可填 Voice ID
当前 5 个角色 voiceId 全部为空（含 voiceProfile: null 或 voiceId: null）
不能自动伪造 Voice ID。 需按 TTS 厂商官方 voice 配置，例如 OpenAI：alloy / echo / fable / onyx / nova / shimmer 等。

E01 对白角色至少要给用到的角色配齐 voiceId。

4. FFmpeg / FFprobe 状态
项	状态
OS
Windows 10 (19045)
ffmpeg / ffprobe
NOT INSTALLED（PATH 无）
.env FFMPEG_PATH / FFPROBE_PATH
未出现或为空（走 PATH）
安装途径
可用 winget：Gyan.FFmpeg
RenderService 无需改；装好 PATH 或配置路径即可。

5. 需要配置的环境变量
变量	当前	说明
AI_PROVIDER / AI_BASE_URL / AI_API_KEY / AI_MODEL
CONFIGURED（DeepSeek 文本）
仅文本 fallback；不能顶 IMAGE/TTS
AI_ENCRYPTION_KEY
CONFIGURED
Provider Key 加密必需
DATABASE_URL
CONFIGURED
PostgreSQL（Docker 健康）
FFMPEG_PATH / FFPROBE_PATH
EMPTY
装进 PATH 后可保持空
没有单独的 IMAGE_API_KEY / TTS_API_KEY。 IMAGE/TTS Key 走 AiProvider 加密字段 + ProjectAiConfig。

报告不输出任何真实 Key。

6. Project AI Config 应如何配置
对项目 demo-xinghe（星河碰撞）：

新建（推荐）或更新一个 OPENAI_COMPATIBLE Provider
支持 Images + Speech 的 baseUrl + Key
Capabilities：至少 IMAGE、TTS（可同一 Provider，也可两个）
挂模型（含 capability 标记）：
IMAGE → 图像模型
TTS → TTS 模型
Project AI Config：
IMAGE → 上述 providerId + model
TTS → 上述 providerId + model
角色 Voice：为 E01 对白角色写入 voiceProfile.voiceId
安装 FFmpeg（winget），确认 ffmpeg -version / ffprobe -version
配置完成后应看到：

IMAGE：READY
TTS：READY
VOICE：READY（角色有 voiceId）
FFmpeg / FFprobe：READY
然后才做 Shot 001 / 首条对白 Smoke Test（不先 Generate All）。

能力总表（以代码为准）
Capability	代码 implemented	星河碰撞 Project 配置	当前可用 Runtime
CHAT
✓
✓ DeepSeek
READY
STRUCTURED_OUTPUT
✓
✓ DeepSeek
READY
IMAGE
✓
✗
NOT READY
TTS
✓
✗
NOT READY
VIDEO / IMAGE_TO_VIDEO / MUSIC / SFX
✓
✗
本阶段不强制
VOICE_CLONE / EMBEDDING
✗
—
N/A
FFmpeg / FFprobe
检测已有
PATH 空
NOT READY
Runtime Provider 种类： 实际可实例化的只有 OPENAI_COMPATIBLE（create-provider.ts）。不要擅自加新 SDK。

诊断 API： 仅有 GET /health；/ai/capabilities + /projects/:id/ai-config 已够做环境判断。本阶段尚未新增 /health/production（审计阶段不改代码）。

继续 Bootstrap 前需要你确认
是否有可用的 OpenAI（或兼容网关）API Key，且支持 Images + TTS？
若有：BaseUrl、IMAGE 模型名、TTS 模型名、可用 Voice ID 列表（可私发，勿贴进公开聊天若敏感）。
是否允许本机执行：winget install Gyan.FFmpeg？
你确认后我再做：装 FFmpeg → 配 Provider/ProjectAiConfig → 配角色 Voice → Shot 001 / TTS Smoke → 再全量与 Render。在拿到真实 IMAGE/TTS 凭证前，不会 Mock，也不会改 E01 业务数据制造完成态。